# Recovery

To be filled during the test deployment.

Required scenarios:

- recover from broken SSH/firewall
- rotate public IP and regenerate links
- promote reserve server
- revoke a compromised client
- rotate SSH keys after Telegram-based audit access

