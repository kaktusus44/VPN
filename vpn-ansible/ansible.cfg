[defaults]
inventory = inventories/test/hosts.yml
roles_path = roles
private_key_file = ~/.ssh/vpn_ansible
host_key_checking = True
retry_files_enabled = False
stdout_callback = default
result_format = yaml
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
